# CORTEX Docker Environment Configuration
# Copy this file or rename to .env.docker for Docker deployments
# These values override the defaults in docker-compose.yml

# Server port (internal container port is always 3005)
PORT=3005

# Environment mode
NODE_ENV=production

# Database path (inside container, mapped to volume)
DB_PATH=/app/data

# CORS origin - set to your frontend URL in production
# Use * for internal testing, specific origin for production
# Examples:
#   CORS_ORIGIN=*
#   CORS_ORIGIN=http://localhost:3005
#   CORS_ORIGIN=https://cortex.stewart.com
CORS_ORIGIN=https://cortex.snowsecure.net