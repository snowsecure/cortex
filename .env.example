# SAIL-IDP Environment Configuration

# Server
PORT=3001
NODE_ENV=development

# Database (restrict filesystem access in production)
DB_PATH=./data

# CORS: in production set to your frontend origin (e.g. https://cortex.example.com).
# Comma-separated for multiple origins. * allows all (server warns in production).
CORS_ORIGIN=*

# Frontend (for production builds; same origin recommended)
VITE_API_URL=http://localhost:3001
